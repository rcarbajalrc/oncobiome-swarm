# OncoBiome Swarm — Production dependencies
# Pin exact versions to prevent supply chain attacks and ensure reproducibility.
# To update: pip install -U <package> && pip freeze | grep <package>

anthropic==0.49.0
pydantic==2.10.6
pydantic-settings==2.7.1
numpy==1.26.4
plotly==5.24.1
dash==2.18.2
python-dotenv==1.0.1
mem0ai==0.1.29
httpx==0.28.1
